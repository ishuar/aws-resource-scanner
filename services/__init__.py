"""
AWS Services Package
-------------------

This package contains modules for scanning different AWS services.
Each service has its own module with a scan function and output processing,
registered in ``services.registry``.
"""
